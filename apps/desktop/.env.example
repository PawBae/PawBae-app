# Supabase 平台凭据（B 线 W3 登录流）。publishable key 公开设计——RLS 才是
# 安全边界；缺失时 App 照常运行，设置页账号区显示「云端未配置」。
# 本地开发：复制为 .env 后填入 Supabase Dashboard → Settings → API 的值。
# 发布构建：release.yml 从仓库 Variables（非 secrets）注入同名变量。
VITE_SUPABASE_URL=https://<project-ref>.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_...
